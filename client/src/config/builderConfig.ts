/**
 * BUILDER_ENABLED: If false, /__builder route shows a disabled screen.
 * BUILDER_DEFAULT_HOME: If true, the root path / redirects to /__builder.
 */
export const BUILDER_CONFIG = {
    BUILDER_ENABLED: true,
    BUILDER_DEFAULT_HOME: true,
};
